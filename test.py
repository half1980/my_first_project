print("Because i can....! And much more!")

def addition(a: float, b: float) -> float:
    """This function is used two add to floating point numbers.

    Args:
        a (float): parameter named a of type float for the function
        b (float): parameter named a of type float for the function

    Returns:
        float: Returns the sum of type float, of the parameters a, b
    """
    return a + b

print(addition(3,5))
